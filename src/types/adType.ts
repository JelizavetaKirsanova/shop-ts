type adType = {
  id: string;
  title: { ee: string; en: string; ru: string };
  description: { ee: string; en: string; ru: string };
  image: string;
  price: string;
  category: string;
  userId: string;
};

export default adType;
